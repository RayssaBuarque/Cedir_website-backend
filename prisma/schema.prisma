// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// Adicionar fonte de banco de dados
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// MODELOS DAS TABELAS

// model User{
//   cpf
//   email
//   nome 
// }
